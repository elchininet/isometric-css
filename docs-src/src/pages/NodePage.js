import React, { Fragment } from 'react';
import { SectionTitle } from '../components/SectionTitle';

export const NodePage = () => {
    return (
        <Fragment>
            <SectionTitle level={1} separator>
                Using the library Node environments
            </SectionTitle>
        </Fragment>
    );
};